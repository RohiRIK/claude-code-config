# Secrets
.env
.env.*
!.env.schema

# OS
.DS_Store
# Databases
*.db
*.db-shm
*.db-wal
# Caches & temp
cache/
debug/
logs/
node-compile-cache/
bunx-*/
paste-cache/
mcp-needs-auth-cache.json
stats-cache.json
settings.json.bak
# Runtime
node_modules/
backups/
package-lock.json
# Claude Code internal (session/runtime data)
history.jsonl
projects/
sessions/
contexts/
tasks/
todos/
transcripts/
telemetry/
statsig/
tmp/
shell-snapshots/
file-history/
security_warnings_state_*.json
# Plans & specs (local working docs)
plans/
specs/
# LTM standalone (migrated to plugin)
memory/
# Context-mode (plugin runtime data)
context-mode/
# Plugins (runtime-managed, contains absolute paths)
plugins/
# Migrations (managed by LTM plugin)
migrations/
# Reports (generated output — never commit)
reports/
# Build artifacts
assets/Arcaived/
# Symlinks (local-only, machine-specific — never commit symlinks to shared tools)
# Rule: ANY symlink inside ~/.claude/ must be listed here. The pre-commit hook
# auto-detects and adds new ones, but always add manually when creating a symlink.
skills/SecurityScan
skills/DataReportBuilder
skills/Agy
skills/Art
skills/BackendDesign
skills/Build
skills/CodebaseOnboarding
skills/CodingStandards
skills/ContextBudget
skills/CreateCLI
skills/CreateMcp
skills/CreateSkill
skills/DockerPatterns
skills/FrontendAesthetics
skills/FrontendDesign
skills/GitHubOps
skills/GitWorkflow
skills/Hygiene
skills/Iterate
skills/IterativeDepth
skills/OpenCode
skills/Orchestrate
skills/Pi
skills/Prompting
skills/Reflect
skills/Research
skills/SecurityReview
skills/Simplify
skills/SkillForge
skills/Spec
skills/StrategicCompact
skills/TddWorkflow
skills/Test
skills/Verify
skills/Workflows
skills/interceptor
skills/interceptor-browser
skills/interceptor-macos
skills/interceptor-research
# Scheduler / job queue runtime state
daemon/
daemon.log
jobs/
# Nested runtime state written when cwd is ~/.claude (scheduler lock, local settings)
.claude/
# Dev infrastructure (local only)
.dev-team/
.last-cleanup
.last-update-result.json
.caveman-active
docs/memory-long-term-dump.md
# Symlink (local-only, auto-blocked by pre-commit)
hooks/.cursor/rules/use-bun-instead-of-node-vite-npm-pnpm.mdc

# Runtime execution telemetry (skills execution log)
state/
