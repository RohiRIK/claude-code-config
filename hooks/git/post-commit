#!/bin/sh
# openltm git-learn hook — no-op if the plugin bundle is absent.
BUNDLE="$HOME/.claude/plugins/marketplaces/OpenLtm/hooks/GitCommit.bundle.mjs"
if [ -f "$BUNDLE" ]; then
  CLAUDE_PLUGIN_ROOT="$HOME/.claude/plugins/marketplaces/OpenLtm" bun "$BUNDLE" "$@"
fi
